# Validation Report

Status: FAIL

## Errors

- DWD dwd_mkt_coupon_usage_df 缺少度量
- DWD dwd_mkt_coupon_usage_df has no measure fields

## Warnings

- DWD dwd_trd_place_order_df 的 FK user_id 未在总线矩阵标记，按业务键匹配到 dim_user
- DWD dwd_trd_place_order_df 的 FK shop_id 未在总线矩阵标记，按业务键匹配到 dim_shop
- DWD dwd_trd_order_detail_df 的 FK sku_id 未在总线矩阵标记，按业务键匹配到 dim_sku
- DWD dwd_trd_payment_df 的 FK user_id 未在总线矩阵标记，按业务键匹配到 dim_user
- DWD dwd_trd_refund_df 的 FK user_id 未在总线矩阵标记，按业务键匹配到 dim_user
- DWD dwd_trd_refund_df 的 FK sku_id 未在总线矩阵标记，按业务键匹配到 dim_sku
- DWD dwd_inv_inventory_snapshot_df 的 FK sku_id 未在总线矩阵标记，按业务键匹配到 dim_sku
- DWD dwd_inv_inventory_snapshot_df 的 FK shop_id 未在总线矩阵标记，按业务键匹配到 dim_shop
- DWD dwd_mkt_coupon_usage_df 的 FK user_id 未在总线矩阵标记，按业务键匹配到 dim_user
- DWD dwd_mkt_coupon_usage_df 的 FK coupon_id 未在总线矩阵标记，按业务键匹配到 dim_coupon
- 业务过程缺少度量: coupon_usage
